# Shark OS secrets file (example)
# Copy to /etc/shark/secrets.env and set permissions: chmod 600 /etc/shark/secrets.env
#
# NEVER store secrets in config.yml or source code!

SHARK_AUTH_TOKEN=changeme-strong-token-here
# Add other secrets as needed