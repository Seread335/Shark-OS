# Maintenance scripts

This folder contains utilities to maintain the repository and build artifacts.

- `clean-build.sh` — remove `dist/`, `/tmp/shark-build`, and `build/` directories.

Usage:

```bash
bash scripts/maintenance/clean-build.sh
```